name: Deploy PHP to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Sync PHP App
    runs-on: ubuntu-latest

    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Sync files to InfinityFree
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: 
          username: if0_42980405
          password: purpleprada4
          local-dir: ./xampp/htdocs/nazareth-opd-system/
          server-dir: ./htdocs/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.github/**
            **/.env
